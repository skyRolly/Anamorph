# ADR Index

Mandatory registry of all Architecture Decision Records. An ADR not listed here is invalid.
ADRs are created **only** when supported by repository evidence (constraint C1); the set below
is the evidence-confirmed result, not a predefined quota. New decisions append the next number.

Status values: Proposed · Accepted · Deprecated · Superseded.

| ID | Title | Status | Evidence confidence |
|---|---|---|---|
| [ADR-0001](ADR-0001-format-agnostic-dsp-core.md) | Format-agnostic DSP core via `EngineParameters` POD | Accepted | Verified |
| [ADR-0002](ADR-0002-parameter-id-versioning.md) | Parameter ID versioning & immutability (`kVersion`) | Accepted | Verified |
| [ADR-0003](ADR-0003-oversampling-strategy.md) | Oversampling wraps nonlinear stages only; minimum-phase IIR; exact PDC | Accepted | Verified |
| [ADR-0004](ADR-0004-clickfree-transition-strategy.md) | Click-free transition strategy (duck / crossfade / warm monitor) | Accepted | Verified |
| [ADR-0005](ADR-0005-phase-matched-dry-reconstruction.md) | Phase-matched dry reconstruction `A(dry)` for the Multiband Mix | Accepted | Verified |
| [ADR-0006](ADR-0006-strict-serial-signal-chain.md) | Strict serial chain: Mono Maker post-Mix, Band Solo post-everything | Accepted | Verified (code) / Partially Verified (history) |
| [ADR-0007](ADR-0007-levelmatch-measure-predict.md) | Level Match = BS.1770 Measure + absolute Predict | Accepted | Verified |
| [ADR-0008](ADR-0008-custom-per-ab-undo.md) | Custom per-A/B-slot Undo/Redo (replaces JUCE UndoManager) | Accepted | Verified |
| [ADR-0009](ADR-0009-nan-selfheal-nyquist-clamp.md) | Crossover Nyquist clamp + engine-wide NaN/Inf self-heal; no output clipper | Accepted | Verified |
| [ADR-0010](ADR-0010-host-hidden-internalstate.md) | Host-hidden `InternalState` for non-musical parameters | Accepted | Verified (code) / Partially Verified (history) |
| [ADR-0011](ADR-0011-linux-x11-cpu-render.md) | Linux/X11 CPU rendering — no OpenGL attach | Accepted | Verified (code) / Partially Verified (history) |
| [ADR-0012](ADR-0012-juce-8.0.14-upgrade.md) | JUCE dependency upgrade 8.0.8 → 8.0.14 | Accepted | Verified (CI build + the then-current 23 tests + pluginval) |
| [ADR-0013](ADR-0013-raw-normalised-serialization-attribute.md) | Additive `raw` normalised value attribute (exact discrete-param state round-trip) | Accepted | Verified (CI `--randomise` state restoration) |
| [ADR-0014](ADR-0014-multiband-bands-solo-automatable.md) | Expose `mbBands`/`mbSolo` to host automation (remove `withAutomatable(false)`) | Accepted | Verified (code) |
| [ADR-0015](ADR-0015-split-drag-zero-latency-follower.md) | Split-movement transitions: zero-latency LR4 retained, rate-capped follower — v0.8.10 final + slow-drag fix: slew-limited smoother under R(f) = 4·max(1, f/300) oct/s, controlled FM over latency (full A–H3 investigation history) | Accepted | Verified (measurements + code + Test 29) |
| [ADR-0021](ADR-0021-build-hardening-strategy.md) | Build Hardening Strategy (RH-PR-2): retain-then-strip symbol pipeline + separate debug artifacts, full RELRO/CFG/stack-protector pinned, artifact/signing failure hygiene; numerics-affecting flags frozen (0016–0020 reserved by RELEASE_HARDENING_PLAN §8) | Accepted | Verified (twin-dump byte-exact + the then-current 130-check suite + binary audit; Windows/macOS steps confirmed by CI) |
| [ADR-0022](ADR-0022-juce-9.0.0-upgrade-sha-pin.md) | JUCE dependency upgrade 8.0.14 → 9.0.0 + immutable-commit (SHA) pinning; Linux EGL build dep | **Accepted** (Architecture Review + Level-5 audition signed off 2026-08-15, against the 9.0.1 build that succeeded this pin) | Verified headlessly (32-scenario twin-dump bit-identical incl. latencies; 140 + 774 suites green; registry snapshot unchanged) / Verified — manual (Level-5 audition; not headlessly reproducible) |
| [ADR-0023](ADR-0023-vendor-manufacturer-code.md) | Vendor manufacturer code `Anmf` → `RTec` (product-line identity; AU component + VST3 UID change, pre-0.9.1 sessions report the plug-in as missing — KI-016); also adds the plugin-identity carve-out to `COMPATIBILITY_POLICY` exception condition 2 | **Accepted** (Architecture Review + Level-5 identity check signed off 2026-07-30) | Verified (code) / Verified — manual (host registration + `auval -v aufx Anmr RTec`; not headlessly reproducible) |
| [ADR-0024](ADR-0024-preset-identity.md) | Factory-preset identity is an immutable internal id (user presets are identified by their file), carried in **plug-in state** so the indicator survives a session reload — 3 additive root fields + 3 per A/B slot; user preset FILES are unchanged, parameter restore is independent of identity restore, and anything unresolvable ticks nothing rather than a same-named substitute | **Accepted** (amended 2026-08-07 pre-merge: the original "never serialized" clause is reversed with maintainer Architecture-Review approval) | Verified (code + state tests 10/11/12; state test 1 pins the new schema shape) |
| [ADR-0025](ADR-0025-regression-test-exception.md) | Documented exception to `TESTING_POLICY` rule 1 for defects with **no stable automated test surface** (GUI/component lifetime, host-owned UI behaviour, OS-level asynchrony) — the default is unchanged and the release gate untouched; invoking it requires four disclosures and registers the gap in `TESTING.md` §Gaps, and the exception lapses when the surface appears | **Accepted** (maintainer instruction 2026-08-07) | Verified (the amended Policy rule; the register and its two pre-existing entries; applied to INC-010) |
| [ADR-0026](ADR-0026-juce-9.0.1-upgrade.md) | JUCE dependency upgrade 9.0.0 → 9.0.1 (immutable-commit pin retained; no source or build-dependency change) | **Accepted** (Architecture Review + Level-5 audition signed off 2026-08-15) | Verified headlessly (32-scenario twin-dump bit-identical incl. latencies; 140 + 894 suites green; registry snapshot unchanged; pluginval 10 both modes ×3) / Verified — manual (Level-5 audition; not headlessly reproducible) |
| [ADR-0027](ADR-0027-cxx23-language-standard.md) | C++ language standard 17 → 23 (`CMAKE_CXX_EXTENSIONS OFF` retained; one `<algorithm>` include added; no version bump; C++20 fallback evaluated and not taken) | **Accepted** (Build System change — Architecture Review signed off 2026-08-15) | Verified (three-platform CI builds incl. AppleClang + MSVC, plus a local libc++ build; 32-scenario twin-dump bit-identical incl. latencies C++17 vs C++23; 140 + 894 suites green under libstdc++ and libc++; identical 29-instance warning set; pluginval 10 both modes ×3) |
| [ADR-0028](ADR-0028-clang-toolchain-pin.md) | CI Clang toolchain pinned to **upstream stable** from **apt.llvm.org**, by major — **22 when decided**; the rule is *upstream stable*, not newest (23 was still at rc then) and not whatever Ubuntu packages; restores `-fsanitize=vptr`, which Clang 21 dropped from `undefined`; governs two jobs that ship nothing; deliberately not bot-updatable. **Amends `ARCHITECTURE_REVIEW_GATE.md`** with the compiler/toolchain rule (*who chooses the version*) that reconciles the AppleClang 15 → 21 precedent. **Value unchanged at 22**; [ADR-0033](ADR-0033-clang-toolchain-pin-23.md) fired this ADR's revisit trigger, evaluated 23, did not take it, and *amends* this ADR by asserting the release identity the install had only assumed | **Accepted** (Build System change — maintainer approval 2026-08-17, with the policy amendment) | Verified (`diff`-identical 52-instance warning census 20 vs 22, so only `# clang-major:` moved — and identical to 18 as well; 140 + 894 suites green under the clang-22 build and again under ASan+UBSan+vptr; LTO/lld link and compile-canary both green at 22; the mismatch guard observed firing; the `vptr` loss reproduced directly under both majors) |
| [ADR-0029](ADR-0029-realtime-enforcement-strategy.md) | Realtime-safety enforcement: **RealtimeSanitizer** at the annotated audio entry point, in its own CI lane with a liveness canary — plus `-Wfunction-effects` **scoped to the JUCE-free leaf layer** (measured 0 there against 52 from opaque JUCE calls on the engine TU, so it is enabled where it is signal and off where it is noise), what deliberately stays out (`-Wperf-constraint-implies-noexcept`, a no-op on an already-`noexcept` entry point) and the two tiers that reach the platforms RTSan cannot (the allocation guard + the static lint, both now shipped). Discharges the RTSan sentence in ADR-0028; amends no Policy — it implements enforcement for `REALTIME_AUDIO_POLICY` | **Accepted** (Build System + validation change — maintainer approval 2026-08-18) | Verified (the real audio path runs violation-free under RTSan across the 156-check suite; a seeded `juce::AudioBuffer` allocation in `process` fails the run at exit 43 naming `AnamorphEngine.cpp:664` through `juce_HeapBlock.h:356`; the annotation is byte-identical in object code at `-O3`; the canary aborts instrumented and is refused uninstrumented; the leaf-layer `-Werror=function-effects` check is clean and still fails on a seeded call to an allocating helper) |
| [ADR-0030](ADR-0030-linux-release-toolchain.md) | Linux **release artifact moves from GCC to Clang** (the pinned `clang-<n>` + its matching `lld-<n>` from apt.llvm.org, which the `-flto` archive link requires; the major is 22, held there by ADR-0033); the duplicate `linux-clang` job folds into `linux` with no check lost; **GCC 16 stays as the compatibility compiler** on a deliberately weak `gcc:16` major-only tag (no package source ships a released GCC 16); `CXXABI` added to the gated ABI floor at GCC 13's value | **Accepted** (Build System change — maintainer approval 2026-08-21) | Verified (Clang Release builds + links via LLD 22.1.8; 162 DSP + 911 state checks pass; ABI `GLIBC_2.38`/`GLIBCXX_3.4.31`/`CXXABI_1.3.9` within floor, and the GCC 16 artifact now correctly FAILS it; Clang warning gate unchanged at 14 sites / 7 entries against the release config; GCC 16 first-party set identical to baseline) |
| [ADR-0031](ADR-0031-x86-64-isa-baseline.md) | x86-64 ISA baseline `-march=haswell -ffp-contract=off` on the **GCC/Clang x86-64 builds only** (Linux x86-64 + the macOS `x86_64` slice via `-Xarch_x86_64`; arm64 and MSVC untouched) — **amends ADR-0021**'s numerics-freeze clause. **Its MSVC exclusion (option 5) is SUPERSEDED by [ADR-0032](ADR-0032-msvc-avx2-baseline.md)**, which adopted `/arch:AVX2` on the MSVC x64 target once the Windows twin-dump instrument discharged option 5's evidence blocker; arm64 remains untouched by option 4, which stands. Class A by construction: `-march=haswell` introduces an FMA instruction the frozen baseline did not have, and `-ffp-contract=off` forbids its use, so the arithmetic sequence is pinned rather than moved. Imposes a **Haswell (2013) / Excavator (2015) ISA floor** with a `SIGILL` failure mode, written into `COMPATIBILITY_POLICY` before the flag landed, together with the statement that bit identity holds **within** an architecture and build configuration, not across architectures | **Accepted** (Build System change — maintainer approval 2026-08-22) | Verified (−17.2 % engine-wide, 1704.9 → 1412.2 Ir/sample at 48 kHz/128 `working`; 32/32 twin-dump scenarios identical to baseline; 0/180 configuration mismatches; 0 FMA instructions emitted vs 707 with contraction left at its default; GCC-haswell and Clang-haswell bit-identical, so the two-toolchain cross-check survives) / To confirm on CI (the `-Xarch_x86_64` form on AppleClang; the Rosetta 2 AVX2 probe verdict) |
| [ADR-0032](ADR-0032-msvc-avx2-baseline.md) | Extend the x86-64 ISA baseline to **Windows/MSVC**: `/arch:AVX2` at the default non-contracting `/fp:precise` (`/fp:contract` NOT enabled; no runtime dispatch) — **supersedes ADR-0031 option 5's deferral** on the evidence its instrument produced. Class A demonstrated on MSVC: `windows-avx2-ab` measured **0/32** twin-dump scenarios moved by the flag (toolset 14.51.36231, two runs) and 32/32 moved by `/fp:contract` — contraction is the bit-moving half, as on GCC/Clang. Both toolset-version properties are asserted per push: the `windows` job requires toolset ≥ 14.30 (and fails on an unreadable version), and `windows-avx2-ab` is a **blocking** baseline == `/arch:AVX2` gate with numerical and infrastructure failures distinctly labelled. The Windows ISA floor (Haswell/Excavator, `STATUS_ILLEGAL_INSTRUCTION`) lands in policy/matrix/KI-026/user guides in the same change. Benefit stated as mechanism-shared; no Windows Ir figure exists or is quoted | **Accepted** (Build System change — explicit maintainer approval 2026-08-30) | Verified (the two-run A/B/C measurement; the doc-verified `/fp` semantics) / To confirm on first post-adoption CI (toolset gate; blocking gate at 0/32 with the flag live, self-checked present-in-B/absent-in-A) |
| [ADR-0033](ADR-0033-clang-toolchain-pin-23.md) | LLVM 23 **evaluated and NOT adopted**; `ANAMORPH_CLANG_VERSION` **stays 22**, and *upstream stable* becomes an **assertion** rather than a major check. Fires ADR-0028's revisit trigger (23.1.0 released 2026-08-25) and **amends** it — nothing is superseded. The blocker is availability, not policy: every apt.llvm.org `-23` suite (noble, resolute, bookworm, trixie) serves `1:23.1.0~++20260818083557+55feb0a3b6b7`, **49 commits before** `llvmorg-23.1.0` (`ea7d852a`), and no Ubuntu series publishes `clang-23` at all. `setup-llvm-apt.sh` now carries a per-major **release identity** (version + `llvmorg-<v>` tag commit), refusing an unrecorded major (exit 2) and reading the build commit from **four** sources — `clang --version` plus the installed version of each of the three packages — where every source carrying a commit must agree, at least one must carry one, and it must be the tag's, else exit 1. The "at least one" rule replaces a first-implementation **bypass** that accepted a missing commit; `--self-test` (9 cases) proves the decision function in `source-lint` and `preflight.sh`. The 23 line is deliberately absent. **This ADR's first draft adopted 23 and was wrong**, corrected in place in the ADR-0028 style. Rules 2–3 have nothing to act on — no compiler moved | **Accepted** (Build System change — maintainer-directed toolchain update 2026-08-30) | Verified (the blocker measured across four codenames + Launchpad + upstream tag ancestry; the gate proven in all three directions — 22 passes naming `ca7933e47d3a` = `llvmorg-22.1.8^{}`, an unrecorded major exits 2, and 23 with its true identity exits 1 naming both commits) / Carried forward, not licensing adoption (32/32 twin-dump identical, `diff`-identical warning census, 226+920 and 224+920 checks, ABI unmoved — all taken against the PRE-RELEASE build) / Unevaluated: upstream's own release tarballs, unreachable from this environment |
| [ADR-0034](ADR-0034-latency-follows-the-oversampling-factor.md) | Reported latency follows the Oversampling **factor**, not the wrap's engagement — `predictLatency`/`getLatencySamples` read `oversample` and nothing else, and a 2-channel integer ring (`osCompDelayBuffer`) stands in for the wrap's group delay in the one state where the wrap is skipped but a factor is selected. **Amends [ADR-0003](ADR-0003-oversampling-strategy.md)**, whose "reported latency is 0 when nothing nonlinear is active" clause this reverses; its engagement rule, its filter choice and the CPU saving are untouched. Fixes a reported host-graph restart on an ordinary Drive or Algorithm move (0 → 4 at 2×, 0 → 6 at 4×/8×). Costs: latency now shows in the host on a linear chain with a factor selected (manual + tooltip corrected), and the latency-crossing set MOVES rather than shrinking — a forced swap crossing the Drive threshold becomes latency-neutral and keeps its dry fill, while one that changes only the FACTOR at Drive 0 becomes latency-crossing and ducks to silence where it used to dry-fill (−2.1 dB → −54.2 dB, permitted since it happens at an Oversampling switch). The duck around the Drive crossing now dry-fills rather than muting, which the latency fix is what makes legal (−53 dB → −0.3 dB). Also removes a second, measured click: while TRUE-BYPASSED the same threshold crossing used to jump the delay-aligned read position at full level (worst sample step 0.07162 / 0.09988 against a 0.01440 smooth bound) | **Accepted** (Latency change — maintainer instruction 2026-09-03) | Verified (Test 52 — 12 of 26 checks fail pre-change, 0 after; Leg C rejects the always-run-the-wrap alternative by 1.31–1.57 absolute, measured on that counterfactual; `--os-latency-probe` matrix; `AnamorphBench` 152–160 ns/sample with the wrap skipped at 2×/4×/8× vs 153.27 at Off and 211–380 engaged) |
| [ADR-0035](ADR-0035-oversampling-path-crossfade.md) | The oversampling **path swap is a crossfade, not a ducked switch** — `osBlend` (12 ms) mixes the base-rate and wrapped paths, `osActiveFor` leaves `discreteDiffers`, the stand-in ring is kept warm on every block, the wrap starts under a ~0 gain, and both paths run from one drive envelope (`envStride`). **Depends on [ADR-0034](ADR-0034-latency-follows-the-oversampling-factor.md)**, which gave the two paths the same latency and so made them mixable at all, and **supersedes its Decision point 5**; **amends [ADR-0004](ADR-0004-clickfree-transition-strategy.md)** by moving this one transition from the duck class to the crossfade class. Fixes a reported interruption on a Drive crossing with Haas / Velvet Noise: the duck's gain lands DOWNSTREAM of those wideners, so the handover's discontinuity entered their 12–35 ms delay lines at full level and re-emerged with the fade already over (2.6× / 5.8× the settled sample step, at duck bottom + the widener's own delay, at 2×/4×/8× alike) | **Accepted** (Signal Flow change — maintainer instruction 2026-09-03) | Verified (Test 53 — 26 of 56 checks fail pre-change, 0 after; all 24 combinations of factor × algorithm × direction × gesture now match their Oversampling-Off control; `AnamorphBench` 166.60–170.06 ns/sample with the wrap skipped vs 159.19 at Off and 210.44–375.85 engaged; the A/B table from `--forced-swap-probe` on three engine versions). **Points 8-9** (final review): a factor->Off switch left the blend in flight across its own duck bottom, weighting a wrapped path that does not exist at Off, so for 12 ms the output was the RAW input with Drive and the mod algorithms absent -- the duck bottom now LANDS the blend, and the mix takes the wrapped path's existence from `currentOversampler()` rather than from the blend's weight (Test 54 -- 12 of 32 checks fail pre-change, 0 after; H3/H1 0.103 -> 0.289 against a 0.288 factor->factor control; `--os-off-probe` over all seven switch directions) |
| [ADR-0036](ADR-0036-program-state-ownership.md) | **Program state is message-thread-owned; host threads exchange immutable snapshots** — resolves **D-2 / RISK-007**. Every piece of program metadata (preset name/identity/baseline, the A/B slot set, the undo history, the committed baseline, the Settings tree) is written and read only on the message thread; an off-message-thread `setStateInformation` applies the sound and the oversampling atomic synchronously and hands the DECODED tail over through one `std::atomic<T*>::exchange` cell the message thread adopts (the D-1 shape with a payload); an off-message-thread `getStateInformation` reads an immutable snapshot the message thread republishes after every mutation, or the host side's own view of a restore it handed over and that is still unadopted. The malformed-value text repair moves before `replaceState`, onto the private copy, so the live APVTS tree is only ever written under JUCE's lock. No mutex, no `callAsync`, no wait, nothing on the audio thread; the message-thread path is unchanged. **Amends `THREADING_POLICY.md`** §Host state calls | **Accepted** (Thread Model change — maintainer instruction 2026-09-03) | Verified (ThreadSanitizer before/after on the four probes, repeated; State tests 37–41; the full state and DSP suites; `check-realtime.py`; `preflight.sh`; the `tsan` CI lane with its liveness canary) |
| [ADR-0037](ADR-0037-legacy-ab-slot-baseline-at-the-boundary.md) | **A legacy A/B slot is migrated at the decode boundary; no baseline is ever a live read** — closes ADR-0036 §22's *recorded, not changed* residual. A pre-0.6.4 slot (params only, `AB@slotA`/`@slotB`) is still read, and what leaves `readSlot` is a canonical `StateSet`: its clean baseline is derived from its own bytes at decode time, so `PresetManager::setMeta`'s live-read fallback is deleted and `applyStateSet`'s statement order stops being load-bearing. **Measured first, not assumed**: 86 062 restore-and-switch cycles through the real path gave 0 signature mismatches between the bytes-only predictor and the live signature, but the session-shaped apply path (`replaceState` + `reassertParameters`) rendered the four log-mapped frequency parameters a *variable* number of times (k ∈ {1,2,3}; `replaceState` flushes the rendered value back into the shared tree, which `reassertParameters` then re-normalises) — so one resolver (`anamorph::sessionNormalisedValue`: `raw` → `value` → default) is shared by the repair, the reassert and a session-shaped predictor `soundSignatureAfterRestoring`, and `reassertParameters` asserts the resolved value from the original tree, once. `restoredSoundSig` (§22) moves to the same predictor and becomes exact by construction. Compatibility **migrated**, not dropped: every legacy read path stays; a re-saved pre-0.6.4 session now records its slot baselines. Not a gate item: no field, parameter, thread, DSP-order or latency change | **Accepted** (maintainer instruction 2026-09-06) | Verified (`worklogs/LEGACY_AB_SLOT_BASELINE_v0.9.7.md` §3 measurement and mechanism; State test 65 + State test 5 on the changed tree, and the mutation pass) |
| [ADR-0038](ADR-0038-gesture-state-is-void-once-topology-moves.md) | **A SpectrumImager gesture is void once the band topology it was defined against moves** — `mouseDown` snapshots `bandCount()`, `mouseDrag`/`mouseUp` cancel the whole gesture when the live count differs, and `removeBand` **refuses** a non-live index instead of clamping it into a live one. **Extends [ADR-0036](ADR-0036-program-state-ownership.md) §25**'s rule — an obsolete writer must not reassert over a newer authority — from the processor to the gesture layer; nothing is superseded. Rejects per-consumer validation on evidence: `dragCrossoverTo` validated and returned and was already safe, while `moveBand` validated its pins and still wrote the unpinned splits from drag-start origins, so a restore's split was overwritten (`900.0 Hz` -> `200.0 Hz`). Rejects a lock (`mbBands` is written from the audio thread), marshalling (larger than the defect, and ADR-0036 chose snapshot exchange), and polling the 24 Hz reconcile (42 ms of continued writing). Both halves are needed: the guard is exact for the message-thread paths but not for host automation or `setStateInformation`, and the refusal makes the window a check cannot reach a no-op rather than a wrong target. **Supersedes what State test 66 legs A-D asserted** — a gesture now stops where it used to continue. Message thread only; no lock, allocation or blocking, and no audio path touched | **Accepted** (maintainer instruction 2026-09-07) | Verified (State test 68 legs A/C/E fail before and pass after, leg B passing throughout as the measurement that rejects per-consumer validation; the centralized guard's mutation fails 3 checks; the inverted mutation — call-site guard deleted, refusal kept — leaves all green; state 2 544 / 0, DSP 396 / 0, memcheck 0 errors, TSan clean; `worklogs/SPECTRUMIMAGER_GESTURE_TOPOLOGY_AUDIT_v0.9.8.md`) |
| [ADR-0039](ADR-0039-a-gesture-owns-the-world-it-was-latched-in.md) | **A gesture owns the world it was latched in** — the snapshot moves from the top of `mouseDown` to the point the identifiers are latched (so the add-area press's OWN `addBandAt` is inside its gesture, not an invalidating change), `removeBand (b, expectedBands)` reads the live count ONCE and refuses unless it is the topology the caller validated against, and `gestureX[3]` extends the same invariant to the SOUND so a whole-sound install at an unchanged count voids the gesture too. **Amends [ADR-0038](ADR-0038-gesture-state-is-void-once-topology-moves.md); supersedes nothing** — its Decision stands, this sharpens WHEN the snapshot is taken, WHAT the safe primitive checks, and WHAT counts as the gesture's world. Rejects a caller-side liveness check by measurement (mutation M7 restores the round-3 check, removes the contract, and leg (c) still fails), rejects injecting `soundSetGen` on coverage (it counts wholesale replacements only, missing automation and undo), and rejects re-seeding `dragOrigX` because that array IS the neighbour spring-back. **Rules on both ADR-0038 residuals:** the value-only restore is a real defect and is fixed; a vanished band's Width and solo bit are PRESERVED, deliberately — `SoloMonitor.cpp:85` masks the solo word to the live count and `MultibandWidth.h:53` uses only `bandCount` widths, so the values are inert while hidden and exact when the count returns, and clearing them would be a gated Parameter/Serialization semantics change. Not a gate item; message thread only, no lock, allocation or blocking | **Accepted** (maintainer instruction 2026-09-07) | Verified (State test 69 legs (a)/(c) and State test 70 legs (a)/(b) fail before and pass after; mutations M1, M2, M3, M4, M6 and M7 each killed by a named leg, M6 being one the previous round could not catch; M5 recorded as having no reachable test; state 2 574 / 0, DSP 396 / 0; `worklogs/SPECTRUMIMAGER_GESTURE_TOPOLOGY_AUDIT_v0.9.8.md`) |
| [ADR-0040](ADR-0040-a-gesture-stores-only-what-it-still-owns.md) | **A gesture stores only what it still owns** — the ownership question moves from HANDLER ENTRY to the STORE: `ownsSplit`/`ownsWidth` are compared adjacent to each store with no call between them, the record is taken from the read-back at that store rather than in a blanket pass, and the first value found not to be owned abandons the rest of the burst. Adds `gestureW[4]` so a width-only change is visible at all, and re-validates the count plus each target before every store of `removeBand`'s and `addBandAt`'s `2N ± 1`-store bursts. **Completes [ADR-0038](ADR-0038-gesture-state-is-void-once-topology-moves.md) and [ADR-0039](ADR-0039-a-gesture-owns-the-world-it-was-latched-in.md); supersedes neither** — they decided WHEN a gesture is void and put it at handler entry, which is right for a change arriving between two mouse events and wrong for one arriving between two stores of the same burst. The mechanism is read out of JUCE, not assumed: `setValueNotifyingHost` stores then dispatches listeners SYNCHRONOUSLY (`juce_AudioProcessorParameter.cpp:59-63`, `:111-121`) and `AudioProcessor::ParameterChangeForwarder` (`juce_AudioProcessor.cpp:1467`) hands every one to the host, so a host write-back re-enters inside our own store — which makes the class closable by construction rather than an unclosable race. Rejects a generation counter because a SILENT writer (`reassertParameters` with `notifyHost=false`) bumps nothing yet moves what the imager reads; rejects reusing `soundReplacement` (ADR-0036 §24) because it does not close reentrancy at all, would block the message thread, and would widen an ADR-0036 scope. Not a gate item; message thread only, no lock, allocation or blocking | **Accepted** (maintainer instruction 2026-09-07) | Verified (State test 71 legs (a), (b), (c) fail before and pass after, with (g) and (h) for the laundering and add-transaction halves; mutations N1, N2a, N2b, N3 and N4 each killed by exactly one named leg; positive controls (d), (e), (f) and State tests 66-70 green throughout; state 2 598 / 0, DSP 396 / 0; `worklogs/SPECTRUMIMAGER_GESTURE_TOPOLOGY_AUDIT_v0.9.8.md` sections 15-21) |
| [ADR-0041](ADR-0041-a-coupled-update-is-all-of-it-or-none-of-it.md) | **A coupled update is all of it or none of it, and ownership is a parameter question** — a conditional store RETURNS whether it committed and its caller abandons the rest of the plan on a refusal (`setBands`/`setSoloMask`/`toggleSoloBit` are now `bool`, and both topology transactions stop at a refused mask store); a refresh that cannot re-anchor everything the next write depends on refreshes nothing, so a wheel tick ENDS an in-flight press instead of adopting a width its `dragGrabDY` predates; the solo click carries the `pressBands` it was aimed at; and ownership moves from display pixels to the **normalised parameter value, compared exactly**. **Completes [ADR-0040](ADR-0040-a-gesture-stores-only-what-it-still-owns.md); supersedes nothing.** The threshold is settled by measurement against the real axis: half a display pixel is 0.30-0.65 % of the frequency (0.19 Hz at 30 Hz, 32 Hz at 10 kHz, 61 Hz at the top) against a parameter with NO interval resolving about 0.00055 Hz at 1 kHz — roughly 59 000x, fully automatable, and until now reclaimable, so pixel space was the wrong ownership primitive (outcome C). Rules the cross-thread partial topology transaction a **bounded, documented trade**: option B is unreachable because the layout lives in eight separate automatable parameters with no single commit point, option C loses to the silent writer (`reassertParameters` with `notifyHost=false` advances nothing yet moves what the imager reads), option D is forbidden because `mbBands` is written from the audio thread. Not a gate item: `mbSolo` and `mbBands` keep their meaning | **Accepted** (maintainer instruction 2026-09-07) | Verified (State test 73 legs (a), (b), (c) fail before and pass after, with (g) for the threshold; mutations P1, P2, P3 and P4 each killed by exactly one named leg; positive controls (d), (e), (f) and State tests 66-72 green throughout; state 2 632 / 0, DSP 396 / 0; `worklogs/SPECTRUMIMAGER_GESTURE_TOPOLOGY_AUDIT_v0.9.8.md` sections 26-33) |
| [ADR-0042](ADR-0042-a-store-is-committed-only-when-the-parameter-says-so.md) | **A store is committed only when the parameter says so** — a conditional store re-reads its own target after the last dispatch it can cause and reports what it finds, and a transaction abandons the rest of its plan when a store a later store DEPENDS ON did not commit. `setBands` and `setSoloMask` now return `stored && bandCount() == want` / `stored && soloMask() == mask`, re-read after `endChangeGesture` — the last instant either can still be believed. `spreadSplits` gives `resetCrossover` and `commitFreqEditor` the ownership shape `writeCrossovers` has carried since ADR-0040, replacing the predicate ADR-0040 condemned (*does the live value differ from MY target?*, which a foreign move answers MORE emphatically) and confirming the primary store before moving anything to make room for it. **Completes [ADR-0041](ADR-0041-a-coupled-update-is-all-of-it-or-none-of-it.md); supersedes nothing** — ADR-0041's rule was right and was implemented only as far as the PRECONDITION, and it converted one of the three spread loops. `setParam` stays `void` deliberately: every caller is a burst leaf whose plan comes from the entry snapshot, or a single store with nothing after it, and aborting at a leaf was MEASURED to be worse than completing because the mask is stored first and is only correct once the count changes. Re-rules the cross-thread partial topology transaction a **bounded, documented trade** with a stronger reason than ADR-0041's: the audio-side reader TEARS too (`PluginParameters.cpp:365-374` reads the ten multiband atomics with ten separate `load()` calls, no seqlock), so a single write-side commit would be re-torn and buys nothing without a threading-model change; option E (compensating rollback) newly evaluated and rejected because it writes a stale value over a newer authority (ADR-0036 §25) and cannot terminate deterministically. Safety is re-derived from the DSP: every split clamped and force-ordered, the count clamped, the mask masked, every continuous quantity smoothed, and `mbBands` — the one discontinuous one — written LAST, which is why the store order is kept. Not a gate item; no threading, parameter or serialization change | **Accepted** (maintainer instruction 2026-09-07) | Verified (State test 74 legs A, B, C, D, I(ii), J(ii) fail before and pass after; mutations M1-M7 each killed by exactly the intended leg; positive controls E, F, G, H, I(i), J(i) and State tests 66-73 green throughout. An adversarial pass over the SHIPPED fix then found two more, both the same rule applied inconsistently and both closed with legs K and L and mutations M8 and M9: `spreadSplits` re-proved the values but not the COUNT, which `was[k]` cannot stand in for because mbBands is a different parameter; and `addBandAt` still owned its splits in PIXELS, the one guard ADR-0041 did not convert, so a 32 Hz move at 10 kHz read as unchanged and the burst wrote over it. A third pass over the TRANSACTION half then corrected two claims and removed one more defect: the silent writer DOES advance a version (`PluginProcessor.cpp:779-780`), so option C is rejected on three real obstacles instead of one false one -- and ADR-0041 is annotated to say so; the burst comments claiming "at most one already-issued store behind" understated the residue, which is eight and six; and the bursts wrote the slots their plan leaves alone, which for the SPLITS was a value change because the plan is carried in pixels, measured at `split0 200.000015259 -> 199.999847412` for a split the user never touched -- both bursts now elide those stores (leg M, mutation M10). State 2 676 / 0, DSP 396 / 0; `worklogs/SPECTRUMIMAGER_GESTURE_TOPOLOGY_AUDIT_v0.9.8.md` sections 36-46) |

## How to add an ADR

1. Confirm the decision is backed by code/test/commit/PR/README evidence.
2. Copy the field structure of an existing ADR (Status, Context, Problem, Options, Decision,
   Consequences, Related code, Evidence + confidence).
3. Assign the next sequential number; add a row here.
4. If the ADR changes a Policy or another ADR, mark the superseded record `Superseded`/`Deprecated`
   and cross-link. See `docs/policies/ADR_POLICY.md`.
