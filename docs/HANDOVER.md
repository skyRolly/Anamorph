# HANDOVER.md

Operational status snapshot for technical handover. Update on every release
(`docs/policies/DOCUMENTATION_LIFECYCLE_POLICY.md`). Facts are Verified from the repository;
fields with no repository evidence are marked `TODO` rather than invented (constraint C7).

Snapshot taken at HEAD `c605fbe` (state-restoration + CI-gate hardening; JUCE 8.0.14).

## Operational status

| Field | Value |
|---|---|
| **Current Version** | 0.8.11 (`CMakeLists.txt:14`; CHANGELOG `[0.8.11]` **consolidated 2026-07-20** — folds the accumulated post-0.8.10 work: PR #60 (crossover follower slow-drag regression fix), PR #61 (192 kHz terminal-snap robustness), PR #62 (performance Wave 3), PR #63 (RH-PR-2 build hardening, `### Security`) and PR #76 (performance Wave 4); the merged security-tooling / CodeQL-autofix PRs #65–#75 are CI- and test-side only (not user-visible, no changelog entry per CHANGELOG_POLICY rule 3). The two maintenance-fix CHANGELOG entries formerly filed under `[0.8.10]` moved to `[0.8.11]` — the released 0.8.10 binaries (PR #59, 2026-07-14) predate them.) Prior: 0.8.10 (**released** with PR #59 — the undo/redo forced-duck dry-fill crossfade + rapid-swap robustness, the multiband flat-recombination crossover fix, the adaptive `FrameClock` display-rate GUI refresh, plus three pre-merge correctness rounds: the split-movement fix (final design after five measured iterations, all recorded in **ADR-0015** incl. the "v0.8.10 final decision" refinement and the post-merge **"Crossover Follower Slow-Drag Regression"** maintenance fix: a slew-limited cutoff smoother — a ~20 ms one-pole demand clamped to the frequency-proportional cap R(f) = 4·max(1, f/300) oct/s — plus a discrete-jump bank crossfade in `MultibandWidth`/`SoloMonitor`; the interim chained-crossfade and one-pole designs were measured audible on pure sines, the 1.25 oct/s + release-consolidation follower was rejected for interaction latency, the flat 4 oct/s cap for pinning normal drags octaves behind the ~90 px/octave display, and the H3 gain-domain proposal failed hostile review on width purity — trade documented as KI-012), the forced-duck dry-fill output-gain latch (Test 30, no spike at extreme Output Gain), the Band Solo alt-click exclusive-solo redesign, the Option-click reset undo fix, and the Apple-Silicon-native tooltip corner fix (KI-011, hardware re-test pending) — all folded into the `[0.8.10]` CHANGELOG entry); earlier 0.8.9 (PR #58, Wave-2 performance H3/H4/H5/H6/H11/H15/ALG-4 + a lifecycle fix). |
| **Branch Strategy** | Feature branch `claude/beautiful-sagan-JAUFI` → PRs into `main`. CI builds every branch; `main` carries shipped versions. (No git tags — see RISK-003.) |
| **Build Status** | Green at v0.8.11 (version bump over the merged PR #60/#61/#62 state — no functional change in the bump itself; local Release build + 136-check DSP self-tests green; pluginval runs on the CI Linux gate — the local sandbox cannot fetch the pluginval release under the egress policy, the same constraint recorded for the JUCE bump). Earlier: v0.8.9 (PRs #55/#56/#57/#58); the **JUCE dependency was bumped 8.0.8 → 8.0.14** (`41acaa7`, ADR-0012) and **verified green by CI** — build + DSP self-tests + `pluginval PASSED at strictness 10` on the Linux gate. The v0.8.10 undo/redo + FrameClock work is behaviour-preserving (single forced swaps byte-identical; GUI message-thread only); the multiband flat-recombination fix intentionally changes the multiband audio (removes the crossover dip) with reported latency unchanged. Build = CMake + JUCE 8.0.14, VST3 [+AU macOS] [+Standalone]. |
| **Test Status** | 33 DSP self-tests **+ an A/B state-restoration clamp guard** (140 individual assertions), and pluginval strictness 10 on the Linux gate in **both modes** (deterministic + `--randomise` ×3, blocking). Newest: `testHaasParkedWarmHistory` (Test 34, performance Wave 4: with Haas selected and Amount settled at exactly 0 under FTZ, every block must pass through bit-untouched, re-engaging on silent input must play back audio recorded WHILE parked — the delay lines must keep recording through the parked fast path — and re-parking must return to bit-transparency), `testSoloColdThroughDrag` (Test 33, performance Wave 3: with nothing soloed, a UI-cadence split drag must leave the solo monitor's settled fast path engaged — bank cold, output buffer bit-untouched — and re-engaging must snap the cutoffs to the freshest drag targets; the pre-Wave-3 gains+cutoffs gate wakes the bank and glides instead of snapping, failing both), `testHighRateCrossoverSnap` (Test 32: a moved crossover must land bitwise-exactly and let the solo monitor go cold at 44.1/48/96/192 kHz — the pre-fix glide float-stalls at 192 kHz 0.4688/0.9375/1.8750/3.75 Hz short of target, never cold, and fails; normal rates double as the unchanged-behavior guard), the Test 29 **normal-drag tracking** checks (slow-drag regression fix: a 6.65 oct/s ≈ 600 px/s drag's band edge must be AT the target 0.1–0.35 s after release on both the multiband and solo-monitor paths — the flat 4 oct/s cap measures 0.47 of full / 0.60 of the width-0 leak and fails both) and `testForcedSwapDuringOrdinaryFadeOut` (Test 31: a forced swap landing mid-ordinary-fade-out keeps forced semantics — the pre-fix engine dropped the request and replays a 0.494-peak stale Haas tail, failing). The JUCE 8.0.14 bump was green on CI (`41acaa7`); also `testMultibandSplitDragNoPitchShift` (Test 29: worst 100 ms pitch chunk < 18 cents — the accepted controlled-FM bound of the R(f) = 4·max(1, f/300) oct/s slew-limited smoother, ADR-0015 final + slow-drag fix — through drags, the whole catch-up and an unbroken tone-crossing crawl (the shipped follower measures ~14; the uncapped ~8 oct/s glide measures ~28 and the bare one-pole ~50, both fail); max spur < −31 dBc, measures −41.3 — the chained bank fades measure −28.5 and the rejected fref=150 variant −27, both fail; discrete jumps land < 200 ms; a released 6-oct flick lands by plain gliding well under a second) and `testDryFillRespectsOutputGain` (Test 30: no dry-fill spike at Output Gain −24 dB — the unscaled fill measures 15.8× and fails). `docs/procedures/TESTING.md`. |
| **Release Status** | Pre-1.0 (0.8.x line). **v0.8.11 changelog-dated 2026-07-20** (consolidated per maintainer instruction — version bump + PRs #60/#61/#62/#63/#76; the Level-5 manual audition remains the human sign-off per RELEASE_POLICY); **v0.8.10 finalized** 2026-07-14 (PR #59); prior v0.8.9 2026-07-12 (PR #58). Distribution = CI artifacts (Linux/Windows/macOS); macOS ad-hoc signed, **not notarized** (KI-002). No formal release tags. |
| **Known Blockers** | None blocking a build/ship. Open items are KI-001…KI-012 (`KNOWN_ISSUES.md`) — none release-blocking (all Low/Medium, confirmed/mitigated/reported-external/fix-pending-retest). KI-009 (REAPER Save Preset focus) is host-specific, pending manual investigation; KI-011 (Apple-Silicon tooltip corners) has its fix applied, pending a hardware visual re-test. |
| **Pending Tasks** | The Wave-3 handover roadmap **shipped as performance Wave 4**, and Wave 4's open items (the fresh-eyes sweep — DSP lenses only, GUI lens still pending — and the small-buffer attribution) **shipped as performance Wave 5** (both PR #76, folded into CHANGELOG `[0.8.11] — 2026-07-20`; all Class A — twin-dump bit-exact, suite 140 checks; see `worklogs/performance/WAVE4_INVESTIGATION.md` + `WAVE5_INVESTIGATION.md`). Next ranked candidates (Wave 5 handover): the K-weighting 4-lane bank (W5-D, largest remaining floor item), the lat==0 mix-ring round-trip elimination (W5-A), the GUI fresh-eyes sweep. **H7 + a LevelMeters gate** stay Category C (→ one Architecture Review), then **Multiband LR4 SIMD**, then **OSD-2** (OS-only). H16 is largely superseded by `FrameClock`. Formal CPU/memory budget numbers remain a TODO (PERFORMANCE_BUDGET); real-DAW host matrix (KI-004/KI-009); adopt git release tags (RISK-003); optional KI-001 warm-bank refinement (needs ADR). **A pre-release hardening program is planned** — `docs/architecture/RELEASE_HARDENING_PLAN.md` (licensing, signing/notarization, installers, tag-triggered release pipeline; recommended first implementation task RH-PR-2 build hardening; licensing work gated on ADR-0016..0020 + human business decisions). |
| **Roadmap** | `TODO: no roadmap is recorded in the repository. Requires project-owner input. (AAX remains Not Supported by decision — COMPATIBILITY_POLICY.)` |
| **Ownership** | `TODO: no owner/team metadata in the repository. Requires project-owner input.` Company of record: RollyTech (`CMakeLists.txt:89`); contact per repository owner. |

## Critical dependencies (with version-lock reasons)

| Dependency | Pin | Version-lock reason |
|---|---|---|
| **JUCE** | `8.0.14` (FetchContent, `CMakeLists.txt:33`) | Framework for all DSP, parameters/state, GUI, and plugin wrappers. An unpinned bump can silently change DSP/latency/state-ABI and the X11 editor path (INC-006 lives in JUCE). Pin = reproducible, audited behaviour. A bump is a Build System change (ADR + Review). The 8.0.8 → 8.0.14 bump is recorded in ADR-0012. See `docs/policies/DEPENDENCY_POLICY.md`, RISK-001. |
| **C++ standard** | C++17 (`CMakeLists.txt:16-18`) | The codebase targets C++17; raising it is a build-contract change. |
| **pluginval** | latest release (downloaded) | The conformance gate (strictness 10). Not vendored; fetched by `scripts/run-pluginval.sh`. |
| Linux system libs | distro (`scripts/setup-linux.sh`) | ALSA/JACK/X11/FreeType/GTK/WebKit/mesa/xvfb for headless build + validation. |

## Documentation ownership (proposed RACI — confirm with project owner)

No team structure exists in the repository, so the following is a **proposed** mapping (governance
guidance, not asserted fact); the project owner should confirm/assign:

| Documentation area | Proposed owner |
|---|---|
| `docs/architecture/` (incl. ADRs, DSP, signal flow) | DSP / Audio engineer |
| `docs/architecture/PARAMETER_*`, `SERIALIZATION_*`, `STATE_*` | Whoever owns the parameter/state surface (compatibility-critical) |
| `docs/procedures/` (BUILD, CI_CD, PACKAGING, RELEASE) | Build / Release engineer |
| `docs/policies/` | Tech lead / maintainer (these are binding) |
| `POSTMORTEMS`, `KNOWN_ISSUES`, `FUTURE_RISKS`, `HANDOVER` | Maintainer (updated per release/incident) |

## First steps for a new maintainer

1. Read `docs/SOURCE_OF_TRUTH.md`, then `docs/policies/AI_AGENT_POLICY.md` (Hard Stop conditions).
2. Build + test locally per `docs/procedures/BUILD.md` / `TESTING.md`.
3. Before touching DSP/parameters/threading/state/latency, read the governing policy + ADR.
4. Keep docs in sync per `docs/policies/DOCUMENTATION_LIFECYCLE_POLICY.md`; refresh this file each release.
